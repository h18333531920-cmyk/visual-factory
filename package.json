{
  "name": "visual-factory-v1",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 8791 --bind 127.0.0.1",
    "build:pages": "node tools/build-recovered-platform-library.js && rm -rf dist && mkdir -p dist && cp index.html styles.css app.js config.js tool-auth-guard.js tool-bridge.js animator.html dist/ && if [ -d public ]; then cp -R public/. dist/; fi && cp -R tools dist/tools"
  }
}
